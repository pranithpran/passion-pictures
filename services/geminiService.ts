/**
 * AI Service disabled for manual operation.
 * These functions are stubs to prevent import errors.
 */

export const generateFastText = async (prompt: string, context: string = ''): Promise<string> => {
  return "";
};

export const generateComplexText = async (prompt: string): Promise<string> => {
  return "";
};

export const editImage = async (base64Image: string, prompt: string): Promise<string | null> => {
  return null;
};

export const searchInformation = async (query: string): Promise<string> => {
  return "";
};